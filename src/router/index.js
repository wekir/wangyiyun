// 导入
import VueRouter from 'vue-router'
// 引入组件


// 创建路由对象
const router = new VueRouter({
  routes: [
    {
      path: '/discover'  //推荐
      // component: 
    }
  ]
})

// 导出
export default router